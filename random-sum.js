const sum = (a, b) => a + b;

const a = Math.floor(Math.random() * 10);
const b = Math.floor(Math.random() * 10);
const result = sum(a, b);

console.log(`
  Yo dawg I heard you like random numbers so I put a random number in your random number so you can random while you random ${a} + ${b} = ${result}.
`);
